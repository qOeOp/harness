# Legacy Surface Audit Redirect

Legacy redirect only.

这个旧路径只为兼容历史链接保留。
canonical workflow 已迁移到 [docs/workflows/surface-audit.md](./surface-audit.md)。
新的默认读序、命名与命令入口统一使用 `surface audit` / `run_surface_diagnostic.sh`。
