# Meeting Router Refs

Read these refs when the `meeting-router` bundle is active.

Recommended order:

1. [docs/workflows/founder-meeting-taxonomy.md](../../../docs/workflows/founder-meeting-taxonomy.md)

Route to exactly one downstream meeting bundle.
If the topic touches volatile external facts, make the downstream bundle honor the `research` bundle `dispatch` gate.
