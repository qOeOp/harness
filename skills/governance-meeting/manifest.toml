schema_version = 1
bundle_slug = "governance-meeting"
bundle_version = 1
bundle_kind = "governance-skill"
summary = "Prepare a governance meeting briefing that combines operating status, process friction, and improvement proposals."

owner_role = "compounding-engineering-lead"
scope = "source-baseline"
maturity = "candidate"

context_strategy = "artifact-first"
delegation_mode = "manager-optional"
entrypoints = ["chat", "review", "work-item"]
activation_modes = ["explicit", "router"]
output_modes = ["summary", "artifact-path"]

allowed_tools = ["read", "write", "web"]
read_roots = ["skills/governance-meeting/", "docs/workflows/", ".harness/workspace/"]
write_roots = [".harness/tasks/<task-id>/attachments/", ".harness/workspace/status/", ".harness/workspace/decisions/log/"]
forbidden_roots = ["roles/", ".harness/workspace/roles/"]

default_artifact_type = "governance-meeting-brief"
default_artifact_path = ".harness/tasks/<task-id>/attachments/"
lazy_load_paths = ["refs/", "templates/"]
